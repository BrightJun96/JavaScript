스프레드는 값이 아닌 값들의 목록이다.

스프레드는 값으로 쓸 수 없다.

스프레드의 사용 가능할 경우
> 함수 호출문의 인수 목록
> 배열 리터럴의 요소 목록
> 객체 리터럴의 프로퍼티 목록

스프레드는 얕은 복사를 하여 새로운 복사본을 생성한다.
    예) const arr = [1, 2, 3];
        const copy = [...arr];
        console.log(arr === copy); => false

객체 리터럴 내부에서는 이터러블이 아닌 객체를 프로퍼티로써 스프레드를 쓸 수 있다.