then 메서드는 언제나 프로미스를 반환한다 =>
then 메서드는 응답 결과를 반환한다

then 메서드의 콜백 함수가 프로미스를 반환하면 그 프로미스도 응답 결과를 반환한다.

then 메서드의 콜백 함수가 프로미스가 아닌 값을 반환해도 암묵적으로 resolve,reject하여 프로미스를 생성해 반환하다.


new promise생성자 함수는 promise 객체를 반환한다 => 요청에 대한 응답값을 반환한다.
promise 객체는 요청에 대한 응답값을 반환한다.

promise를 반환한다는 것은 비동기 처리에 대한 응답값을 반환한다는 것

then 메서드도 promise를 전달받으며 promise를 반환한다. =>  요청에 대한 응답값을 반환한다.