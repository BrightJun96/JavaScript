1. 함수는 객체이다. (o , x)

해설 . 합수는 일급객체의 조건을 만족하므로 객체이다.



2. 다음 중 함수에 관하여 옳지 않은 것은?
    a. 함수는 값처럼 자유롭게 사용할 수 있다.
    b. 함수는 프로퍼티 값과 배열의 요소로도 쓰일 수 있다.
    c. 함수 이름은 함수 몸체 외부에서도 참조할 수 있는 식별자이다.
- 



3. 함수 선언문에 관하여 옳지 않은 것은?

    a. 함수 선언문은 함수 이름을 생략할 수 없다.
    b. 함수 선언문은 변수에 할당할 수 없다.
    c. 함수 선언문도 코드 문맥에 따라 표현식처럼 해석될 수 있다.
    d. 함수 선언문을 선언하면 해당 함수가 실행된다.



4. 위 코드의 a 줄에 foo()는 함수 이름으로 호출한 것이다. (o , x) => x
    자바스크립트 엔진이 생성한 식별자로 호출한 것이다.
    함수 이름은 함수 몸체 내부에서 호출할 수 있다.


- 함수 표현식에서 함수 이름은 생략할 수 있다.




5. 함수 선언문과 함수 표현식으로 생성된 함수 둘 다 함수 호이스팅이 된다. (o , x)




6. 화살표 함수는 기존 함수 선언문 또는 함수 표현식의 동작을 간략화하고 내부동작 또한 기존함수와 동일하다. (o , x)



7. 다음 예제의 콘솔 출력 값은? Q2.js파일
    a. 2
    b. NaN
    c. undefined
    d. null


8. 함수 외부에서 함수 내부로 전달한 객체를 함수 내부에서 변경하면 함수 외부의 객체가 변경된다. (o , x)
 => 부수효과(side effect)


- 매개변수는 함수 몸체 외부에서 참조할 수 없다.
- return 은 실행결과를 외부로 반환할 수 있다.