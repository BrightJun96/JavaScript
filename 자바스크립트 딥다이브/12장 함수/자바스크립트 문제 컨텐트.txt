1. 함수 이름은 함수 몸체 내에서만 참조할 수 있는 식별자이다. (o,x)

2. 함수는 함수 이름으로 호출하는 것이 아니라
   함수 객체를 가리키는 식별자로 호출하는 것이다.(o,x)

    - 설명
        자바스크립트 엔진은 생성된 함수를 호출하기 위해 함수 이름과 동일한 이름의 식별자를 암묵적으로 생성하고,
        거기에 객체를 할당한다.


3. 함수 선언문은 값으로 평가될 수 있다. (o,x)


4. 함수 선언문과 함수표현식의 차이는 무엇인가?  (오지선다 중 맞는 것 고르기?)


5. 함수 선언문은 함수 이름을 생략할 수 없다. (o,x)


6. 함수 선언문은 개발자 도구에 입력하면 어떠한 값이 반환이 되는가?(오지선다 중 맞는 것)


7. 함수 표현식에서는 함수 이름을 생략할 수 있다.(o,x)


8. 함수는 객체이다. (o,x)

    - 설명
        함수는 자바스크립트엔진에 의하여 평가되어 값을 생성하고 이 값은 객체이다.
        즉, 함수는 객체이다. 또한 그중에서도 함수는 일급 객체이다

            * 일급객체
                함수를 값처럼 자유롭게 사용할 수 있다는 의미


9. 그룹 연산자()에 있는 함수 선언문은 함수표현식으로 해석된다. (o,x)


10. 함수 선언문으로 정의한 함수는 함수 선언문 이전에 호출할 수 있다. 


11. 함수 표현식으로 정의한 함수는 함수 표현식 이전에 호출할 수 없다. 
- 함수표현식은 호이스팅이 되지 않는다.


plus. 호이스팅에 관한 설명 (오지선다)


12. 화살표 함수는 일반함수와 똑같이 동작한다. (o,x)


13. function add(x,y) {
    return x + y;
}

    console.log(add(2))의 값은? (사지선다)

    a. 2
    b. undefined
    c. NaN
    d. null


14. 자바스크립트 함수는 매개변수와 인수의 개수가 일치하는지 확인하지 않는다.


15. 매개변수는 순서에 의미가 있다.


16. 함수 외부에서 내부로 전달한 객체를 함수 내부에서 변경하면 부수효과가 발생한다.


17. 함수 내부에 return이 없는 함수를 호출하면 undefined가 반환된다.


18. 함수 외부의 객체를 함수의 인수로서 사용하면 원본 객체값이 훼손된다.


19. 함수 외부의 원시값을 함수의 인수로서 사용하면 원본 원시값이 훼손된다.(o,x)


20. 함수의 매개변수를 통해 외부함수를 전달할 수 있다. 이를 콜백함수라 한다.
    매개변수를 통해 함수의 외부에서 콜백 함수를 전달받은 함수를 고차 함수라고 한다.
    콜백함수는 고차 함수에 의해 호출된다.
    따라서 고차함수에 콜백 함수를 전달할 때는 콜백함수를 호출하지 않고 함수 자체를 전달해야한다.