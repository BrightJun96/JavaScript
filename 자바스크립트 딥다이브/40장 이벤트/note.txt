이벤트 전파는 DOM 요소 노드에서 발생한 이벤트 객체가 DOM 트리에 따라 전파되는 것을 말합니다.
이벤트 객체가 전파되는 단계를 말하자면

이벤트 캡처링은 이벤트 객체가 window에서 시작하여 이벤트 타깃 방향으로 이동하는 단계이고
타깃 단계는 이벤트 객체가 이벤트 타깃에 도달했을 때의 단계이며
이벤트 버블링은 도달한 뒤 window 방향으로 이동하는 단계입니다.
즉, 이벤트 객체는 DOM 트리를 돌아다니며 전파되기 때문에 지정된 요소외에서도
이벤트 타깃을 캐치할 수 있습니다.

이러한 작동원리를 이용하여 이벤트를 발생시킨 요소에서 뿐만 아니라 상위 DOM요소에서도 이벤트 객체를 캐치함으로써
이벤트 타깃을 캐치할 수 있습니다. 
이는 많은 DOM요소에 이벤트 핸들러를 등록해야할 경우 이들의 부모인 상위 요소에
이벤트핸들러를 등록해줌으로써 생산적인 코드를 구현할 수 있습니다.
이를 이벤트 위임이라 합니다.


Q.
그렇다면 이벤트 버블링이 완료된 뒤에 이벤트 핸들러 함수가 호출되는 것인가?


Q.
이벤트 버블링에서 이벤트 타깃을 캡쳐함으로써 얻을 수 있는 이득은?


Q.
이벤트 캡처링에서 이벤트를 캐치하는 것보다
이벤트 버블링에서 이벤트를 캐치하는 것이 좋은 곳인가?

Q.이벤트 위임을 이용하여 하위 DOM 요소들을 캐치할 때  하위 DOM요소와 
상위 DOM 요소가 떨어진 정도가 크다면 안좋은 것인가?



이벤트 위임:
캐치하고 싶은 이벤트 타깃 상위요소에 이벤트 핸들러를 등록하는 것을 말합니다.

