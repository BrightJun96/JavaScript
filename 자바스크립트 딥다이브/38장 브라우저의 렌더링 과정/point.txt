> 서버에서 응답해준 HTML 문서에 대한 바이트 코드는 브라우저가 meta태그의 charset 어트리뷰트에 의거하여
텍스트로 변환한다.

> 토큰: 텍스트(문자열)을 문법을 가진 최소한의 단위로 분해한 요소

> 노드 : 토큰을 객체화한 요소

> 브라우저는 HTML을 순차적으로 파싱하여 DOM을 생성한다. ( 한줄씩 해석하여 DOM 생성)

> 파싱도중 CSS LINK 태그나 JS SCRIPT 태그를 만나게 되면 잠시 멈추어
서버에게 해당 파일을 요청받고 해당 파일을 파싱한다.

> 자바스크립트 파싱과 실행은 브라우저 엔진이 아닌 자바스크립트 엔진이 처리한다.

> 자바스크립트 코드에서 DOM과 CCSOM을 변경하는 DOM API를 사용한다면
리플로우 , 리페인트한다.

> script 태그는 바디 태그의 닫는 태그 바로 앞쪽에 위치시키는 것이 좋다.
DOM 형성된 이후에 접근할 수 있기 때문이다.

다른 방법으로는 script 태그에 async/defer 어트리뷰트를 사용하면 된다.
(defer : 미루다/연기하다.
)