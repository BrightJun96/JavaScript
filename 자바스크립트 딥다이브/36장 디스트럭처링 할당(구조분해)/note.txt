구조 분해 할당 : 구조화된 배열과 같은 이터러블 객체나 객체 리터럴을 분해하여 
1개 이상의 변수에 개별적으로 할당하는 것을 말함.

배열, 객체 리터럴 등에서 필요한 값만 추출하여 쓸 때 유용

우변에는 이터러블이어야하며, 할당 기준은 배열의 인덱스이다.
즉, 순서대로 할당된다.


객체 구조 분해에서는 할당 기준이 순서가 아닌 프로퍼티 키이다.
즉, 순서에 의미가 없으며 선언된 변수이름과 프로퍼티 키가 일치하면 할당된다.